# 真机前最终联调执行清单

更新时间：2026-04-01

## 当前环境

- 前端：Vue 3 + Vite + Vant
- 后端：NestJS + Prisma + MySQL
- 本地后端地址：`http://127.0.0.1:3000`
- 本地前端构建：已通过
- 本地后端构建：已通过

## 已执行检查

### 1. 登录与角色分流

- `operator / Operator@123` 登录成功
- `warehouse / Warehouse@123` 登录成功
- 运营端账号访问仓库接口时返回 `403`
- 仓库端账号访问运营端接口时返回 `403`

结论：通过

### 2. 运营端新建订单

- 可创建订单
- 新订单自动生成 `ZN + 日期 + 当日流水号`
- 自动生成公开访问 token
- 默认状态为 `pending_shipment`

本轮验证订单：

- `ZN20260401004`
- `ZN20260401005`
- `ZN20260401006`

结论：通过

### 3. 图片上传

- 上传接口：`POST /api/v1/uploads/images`
- 测试文件：1x1 png
- 上传成功并返回文件记录
- 静态文件地址可直接访问，HTTP 状态 `200`
- 上传后的图片可绑定到订单详情

示例上传地址：

- `/uploads/images/1775035720205-849fa1aac1a6.png`

结论：通过

注意：

- PowerShell 自带 `Invoke-RestMethod -Form` 可能不会自动带上正确图片 MIME
- 浏览器和微信内置浏览器的真实上传行为正常路径应为 `multipart/form-data`
- 真机测试时仍建议实际点选相册再验证一次

### 4. 老客户识别

- 按手机号可识别历史客户
- 可返回最近收货资料
- 客户详情页在未命中客户时不再错误显示空白信息

结论：通过

### 5. 订单列表与今日订单页搜索

- 订单列表支持关键词搜索：
  - 订单号
  - 手机号
  - 客户名
  - 商品名
  - 商品规格
- 今日订单页支持 `todayOnly + keyword`
- 仓库待发货页支持关键词搜索

结论：通过

### 6. 运营端订单详情页

- 可查看完整订单信息
- 可查看金额拆分
- 可查看商品图
- 可进入编辑弹层
- 可作废 `draft / pending_shipment` 订单

结论：通过

### 7. 作废订单

- 接口：`POST /api/v1/orders/:id/cancel`
- 可将待发货订单改为 `cancelled`
- 作废后详情页状态正确
- 今日统计中的取消数会同步变化

结论：通过

### 8. 仓库待发货页与发货页

- 仓库待发货列表可正常返回
- 仓库发货页改为专用详情接口，不再依赖列表缓存
- 仓库页可看到创建阶段仓库备注
- 兼容真实换行和转义换行备注

结论：通过

### 9. 发货流程

- 接口：`POST /api/v1/warehouse/orders/:id/ship`
- 填写快递公司、运单号后可正常发货
- 发货后订单状态变为 `shipped`
- `courierCompany`、`trackingNo`、`shippedAt` 均会写入

本轮验证：

- 订单：`ZN20260401006`
- 快递公司：`顺丰速运`
- 运单号：`SF-FINAL-0001`

结论：通过

### 10. 客户公开订单查看页

- 访问方式：`/client/orders/:orderNo?token=...`
- 正确 token 可查看本次订单
- 发货后可看到物流信息
- 不返回内部备注
- 不返回仓库备注
- 错误 token 返回 `400`

结论：通过

## 本轮关键执行结果

- 上传成功：`true`
- 上传静态文件访问：`200`
- 新建订单图片数：`1`
- 仓库详情可见创建阶段仓库备注：`请检查尺码`
- 发货后公开订单状态：`shipped`
- 发货后公开订单运单号：`SF-FINAL-0001`
- 客户公开订单不包含内部备注字段：`true`
- 错误 token 返回：`400`
- 仓库访问运营接口返回：`403`
- 运营访问仓库接口返回：`403`

## 真机前仍建议手测的项目

### 微信内置浏览器重点手测

- 相册上传是否正常触发
- iOS 微信上传 HEIC 图片时是否被后端拒绝
- 安卓微信长地址栏分享时 token 是否完整
- 页面返回后表单草稿是否仍在
- 输入框弹起键盘后底部按钮是否被遮挡
- 长地址、长备注、多个商品时页面滚动是否正常

### 页面点击级手测建议

1. 登录运营账号
2. 录入一笔带图片订单
3. 查看详情页并复制客户链接
4. 用客户链接打开本次订单页
5. 登录仓库账号查看待发货列表
6. 进入发货页确认发货
7. 再次打开客户链接确认物流已更新
8. 新建一笔订单后走作废流程

## 当前结论

当前项目已经达到“真机前本地联调通过”的状态，主业务链路已打通：

- 登录
- 录单
- 上传图片
- 老客户识别
- 订单列表
- 订单详情
- 作废订单
- 仓库待发货
- 仓库发货
- 客户单链接查看

剩余风险主要集中在真实手机和微信内置浏览器交互层，不在当前本地接口联调层。
